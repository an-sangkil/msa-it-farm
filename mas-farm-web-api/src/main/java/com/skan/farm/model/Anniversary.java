package com.skan.farm.model;

import java.io.Serializable;
import java.util.Date;

/**
 * 기념일 모델 클래스.
 * 
 * @author generated by ERMaster
 * @version $Id$
 */
public class Anniversary implements Serializable {

	/** serialVersionUID. */
	private static final long serialVersionUID = 1L;

	/** 주소록 관리. */
	private AddressManagement hmMgNoaddressManagement;

	/** 순번. */
	private Long hmAmNo;

	/** 날짜. */
	private Date hmDate;

	/** 구분. */
	private String hmType;

	/** 주소록 관리. */
	private AddressManagement addressManagement;

	/**
	 * 생성자.
	 */
	public Anniversary() {
	}

	/**
	 * 주소록 관리을 설정합니다..
	 * 
	 * @param hmMgNoaddressManagement
	 *            주소록 관리
	 */
	public void setHmMgNoaddressManagement(AddressManagement hmMgNoaddressManagement) {
		this.hmMgNoaddressManagement = hmMgNoaddressManagement;
	}

	/**
	 * 주소록 관리을 가져옵니다..
	 * 
	 * @return 주소록 관리
	 */
	public AddressManagement getHmMgNoaddressManagement() {
		return this.hmMgNoaddressManagement;
	}

	/**
	 * 순번을 설정합니다..
	 * 
	 * @param hmAmNo
	 *            순번
	 */
	public void setHmAmNo(Long hmAmNo) {
		this.hmAmNo = hmAmNo;
	}

	/**
	 * 순번을 가져옵니다..
	 * 
	 * @return 순번
	 */
	public Long getHmAmNo() {
		return this.hmAmNo;
	}

	/**
	 * 날짜을 설정합니다..
	 * 
	 * @param hmDate
	 *            날짜
	 */
	public void setHmDate(Date hmDate) {
		this.hmDate = hmDate;
	}

	/**
	 * 날짜을 가져옵니다..
	 * 
	 * @return 날짜
	 */
	public Date getHmDate() {
		return this.hmDate;
	}

	/**
	 * 구분을 설정합니다..
	 * 
	 * @param hmType
	 *            구분
	 */
	public void setHmType(String hmType) {
		this.hmType = hmType;
	}

	/**
	 * 구분을 가져옵니다..
	 * 
	 * @return 구분
	 */
	public String getHmType() {
		return this.hmType;
	}

	/**
	 * 주소록 관리을 설정합니다..
	 * 
	 * @param addressManagement
	 *            주소록 관리
	 */
	public void setAddressManagement(AddressManagement addressManagement) {
		this.addressManagement = addressManagement;
	}

	/**
	 * 주소록 관리을 가져옵니다..
	 * 
	 * @return 주소록 관리
	 */
	public AddressManagement getAddressManagement() {
		return this.addressManagement;
	}

	/**
	 * {@inheritDoc}
	 */
	@Override
	public int hashCode() {
		final int prime = 31;
		int result = 1;
		result = prime * result + ((hmMgNoaddressManagement == null) ? 0 : hmMgNoaddressManagement.hashCode());
		result = prime * result + ((hmAmNo == null) ? 0 : hmAmNo.hashCode());
		result = prime * result + ((addressManagement == null) ? 0 : addressManagement.hashCode());
		return result;
	}

	/**
	 * {@inheritDoc}
	 */
	@Override
	public boolean equals(Object obj) {
		if (this == obj) {
			return true;
		}
		if (obj == null) {
			return false;
		}
		if (getClass() != obj.getClass()) {
			return false;
		}
		Anniversary other = (Anniversary) obj;
		if (hmMgNoaddressManagement == null) {
			if (other.hmMgNoaddressManagement != null) {
				return false;
			}
		} else if (!hmMgNoaddressManagement.equals(other.hmMgNoaddressManagement)) {
			return false;
		}
		if (hmAmNo == null) {
			if (other.hmAmNo != null) {
				return false;
			}
		} else if (!hmAmNo.equals(other.hmAmNo)) {
			return false;
		}
		if (addressManagement == null) {
			if (other.addressManagement != null) {
				return false;
			}
		} else if (!addressManagement.equals(other.addressManagement)) {
			return false;
		}
		return true;
	}

}
