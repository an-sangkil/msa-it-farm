server:
  port: 8000

spring:
  boot:
    admin:
      client:
        url: http://localhost:9000
        instance:
          name: it-farm-client   #(어플리케이션 명)
          metadata:
            user.name: admin
            user.password: apwhaleldj1!


  security:
    user:
      name: admin
      password: apwhaleldj1!
management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: always

logging:
  file:
    name: logs/it.farm.client.log
  level:
    root: info
    com.skan: debug
    org.springframework : info
    web: debug