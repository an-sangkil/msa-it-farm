##########################################################################################
# 클라우드 컨피그레이션 참고
# https://cloud.spring.io/spring-cloud-config/reference/html/
##########################################################################################

spring:
  application:
    name: itfarmserver
  profiles:
    active: dev
---
spring:
  profiles: local
  cloud:
    config:
      enabled: false
---
spring:
  profiles: dev
  cloud:
    config:
      uri: http://localhost:8888


